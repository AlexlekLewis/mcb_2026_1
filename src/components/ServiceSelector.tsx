import { Ruler, Hammer } from "lucide-react";
import Link from "next/link";

export function ServiceSelector() {
    return (
        <section className="hidden">
            {/* Section removed as per client request */}
        </section>
    );
}
